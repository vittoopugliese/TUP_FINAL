<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_inspection"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    app:strokeWidth="2dp"
    app:strokeColor="#9E9E9E">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Indicador de color (barra vertical izquierda) -->
        <View
            android:id="@+id/view_status_indicator"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:background="#9E9E9E"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Nombre del edificio -->
        <TextView
            android:id="@+id/text_building"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textSize="16sp"
            android:textStyle="bold"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="Edificio Torre Norte"
            app:layout_constraintEnd_toStartOf="@id/chip_status"
            app:layout_constraintStart_toEndOf="@id/view_status_indicator"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Badge de estado -->
        <TextView
            android:id="@+id/chip_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="10dp"
            android:paddingVertical="3dp"
            android:textSize="11sp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:background="@drawable/bg_status_badge"
            tools:text="PENDIENTE"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Fecha programada -->
        <ImageView
            android:id="@+id/icon_date"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginStart="12dp"
            android:layout_marginTop="8dp"
            android:src="@android:drawable/ic_menu_my_calendar"
            android:contentDescription="@string/inspection_scheduled_date"
            app:tint="?android:attr/textColorSecondary"
            app:layout_constraintStart_toEndOf="@id/view_status_indicator"
            app:layout_constraintTop_toBottomOf="@id/text_building" />

        <TextView
            android:id="@+id/text_date"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:textSize="13sp"
            android:textColor="?android:attr/textColorSecondary"
            tools:text="15 Mar 2026"
            app:layout_constraintStart_toEndOf="@id/icon_date"
            app:layout_constraintTop_toTopOf="@id/icon_date"
            app:layout_constraintBottom_toBottomOf="@id/icon_date"
            app:layout_constraintEnd_toStartOf="@id/text_days_remaining" />

        <!-- Días restantes -->
        <TextView
            android:id="@+id/text_days_remaining"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textStyle="bold"
            tools:text="3 días"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/icon_date"
            app:layout_constraintBottom_toBottomOf="@id/icon_date" />

        <!-- Tipo de inspección -->
        <TextView
            android:id="@+id/text_type"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            android:textSize="12sp"
            android:textColor="?android:attr/textColorSecondary"
            tools:text="Mensual"
            app:layout_constraintStart_toEndOf="@id/view_status_indicator"
            app:layout_constraintTop_toBottomOf="@id/icon_date"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
